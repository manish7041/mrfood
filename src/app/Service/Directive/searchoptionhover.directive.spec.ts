import { SearchoptionhoverDirective } from './searchoptionhover.directive';

describe('SearchoptionhoverDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchoptionhoverDirective();
    expect(directive).toBeTruthy();
  });
});
