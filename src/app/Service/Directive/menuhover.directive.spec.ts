import { MenuhoverDirective } from './menuhover.directive';

describe('MenuhoverDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuhoverDirective();
    expect(directive).toBeTruthy();
  });
});
