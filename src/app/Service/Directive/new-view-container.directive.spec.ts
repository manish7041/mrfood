import { NewViewContainerDirective } from './new-view-container.directive';

describe('NewViewContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new NewViewContainerDirective();
    expect(directive).toBeTruthy();
  });
});
