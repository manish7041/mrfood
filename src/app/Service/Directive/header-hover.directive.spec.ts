import { HeaderHoverDirective } from './header-hover.directive';

describe('HeaderHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderHoverDirective();
    expect(directive).toBeTruthy();
  });
});
